<%# code to add the main heading element and giving it a value the same as the
title variable %> <% if (title) { %>
<h1><%- title %></h1>
<% } else {res.redirect('/')}%> <%- messages() %> <% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<% } %>

  <p class="noUnderline centerAndLarge">Welcome to Admin Management</p>

  <p class="centerAndLarge bold">List of Account Users</p>
  <table id="AccountsDisplay"><%- accountsList %></table>

<noscript>JavaScript must be enabled to use this page.</noscript>
<script src="../../js/admin.js"></script>




